# A股量价选股 Agent —— 架构与使用导览（新手版）

> 这份文档解释你拥有的这套系统：整体架构、每个模块干什么、数据存在哪、
> 怎么跑、怎么看结果、怎么调参。配合阅读：`config.example.yaml`（参数总控台）。

---

## 1. 这套系统一句话是什么

**每天收盘后，自动从全市场几千只 A股里，挑出"当前形态最像历史上那些『刚启动就大涨』的股票"的前 5 只，配上文字分析，图文推送到你微信。**

它不是一个黑盒模型，而是一条可解释的流水线：
`数据 → 学形态 → 找相似 → 打分排序 → 排雷过滤 → 文案 → 图文 → 推送 → 每天自动跑`

---

## 2. 整条流水线（谁先谁后）

```
① 数据   akshare 拉全A日线(约1年)          → data/screener.db
② 学习   从"过去10日大涨≥9%的股票"里，
         截取其"启动前25根"存成模板          → template 表（形态记忆库）
③ 匹配   每只候选股拿"当前25根"与模板库比对  → 算出形态相似分 0~100
④ 排雷   避雷针(上影线过长)/爆量/暴热板块/ST/涨停 剔除
⑤ 打分   形态分 ≥ 60 中取 Top5
⑥ 文案   DeepSeek 写"为什么像启动初期"（失败有模板兜底）
⑦ 图文   每只 = K线图 + 中文说明，拼成卡片
⑧ 推送   Top3 卡片图 + Top5 摘要 → 你微信（Server酱）
⑨ 定时   GitHub Actions 每天 15:45 自动执行 ①~⑧
```

对应代码里就是 **4 个 Phase**：数据(0) → 学形态(1) → 选股(2) → 图文推送(3)。

---

## 3. 三个"智能"部件的关系（最重要的心智模型）

| 部件 | 文件 | 干的事 | 通俗比喻 |
|---|---|---|---|
| **形态学习** | `src/patterns/templates.py` | 从历史赢家抽"启动前形态"存模板 | 老兵把"开战前的布阵图"存档 |
| **形态匹配** | `src/patterns/similarity.py` + `src/screen/scorer.py` | 拿今天的阵型去比对档案，找最像的 | 参谋翻档案说"这阵型像上次打胜仗前" |
| **7因子层** | `src/screen/factors.py` | 资金效率/放量/板块联动等打分 + 排雷 | 额外的军规过滤（**回测发现只配做排雷，不配打分**） |

**重要结论（回测得出的，别忘）**：
- 纯"形态相似"选出的股票，未来10日命中率 ~69%，跑赢随机 17~22pct —— **有效**；
- "追涨因子"(F1资金效率/F4当日异动/F5放量) 和趋势分是**反指**——把它们混进打分会把命中率从 69% 砸到 39%；
- 所以现在**打分权重 100% 给了形态**，因子只留"排雷过滤"功能。

---

## 4. 目录结构（每块干什么）

```text
a-share-screener/
├─ config.example.yaml      ★参数总控台：所有可调参数在这改（CI 用它）
├─ config.yaml              本地覆盖（个人临时差异，没提交 GitHub）
├─ requirements.txt         第三方依赖清单
├─ deploy.ps1               一键推代码到 GitHub
├─ DEPLOY.md                部署+每日自动指南
│
├─ src/                     核心代码（逻辑都在这）
│  ├─ config.py             读配置 + 密钥只走环境变量
│  ├─ data/                 数据层
│  │  ├─ fetcher.py         akshare 拉行情：多源降级/重试/限速/增量
│  │  ├─ store.py           SQLite 读写（所有表）
│  │  └─ universe.py        股票池：板块分类/ST识别
│  ├─ patterns/             形态学习（Phase1）
│  │  ├─ templates.py       ★锚点定义 + 模板提取 + 落库
│  │  └─ similarity.py      ★相似度计算（Pearson+量比）
│  ├─ signals/              旧版5信号（已被7因子取代，留作回退）
│  ├─ screen/               选股层（Phase2）
│  │  ├─ scorer.py          ★打分+涨停过滤+行业去重+TopN
│  │  └─ factors.py         7因子体系 + 排雷规则
│  ├─ report/               输出层（Phase3）
│  │  ├─ narrative.py       LLM 形态文案（无key自动兜底）
│  │  ├─ charts.py          mplfinance K线图
│  │  ├─ card.py            Pillow 拼图文卡片
│  │  ├─ fonts.py           中文字体定位
│  │  └─ pipeline.py        汇总流水线：选股结果→文案→图→摘要→推送
│  └─ push/                 推送层
│     ├─ serverchan.py      Server酱（推自己微信，主通道）
│     ├─ wecom.py           企业微信群机器人（备用/群推）
│     ├─ imagehost.py       图床：GitHub/jsDelivr 直链 or sm.ms
│     └─ notifier.py        通道分发 + 告警
│
├─ scripts/                 命令入口（你手动敲的就这些）
│  ├─ init_db.py            建库
│  ├─ backfill.py           首次/增量回填历史数据
│  ├─ update_templates.py   学形态：重建模板库
│  ├─ run_daily.py          ★每日主流程（一步全干）
│  ├─ pick_top.py           快速看某天 Top5（不联网）
│  ├─ validate.py           ★回放验证：信号到底灵不灵
│  ├─ visualize_templates.py 画模板长啥样
│  ├─ compare_pattern.py    当前股 vs 最像模板 对比图
│  ├─ ascii_compare.py      文字版形态对比（无图也能看）
│  ├─ emit_report.py        出图文报告/推送
│  └─ test_push.py          推送通道自检
│
├─ tests/                   离线冒烟测试（99项，改代码后跑一遍防回归）
├─ .github/workflows/daily-screener.yml   ★每日自动任务
└─ data/  output/           数据库 / 生成的图和报告（gitignore）
```

**阅读顺序建议**：`config.example.yaml` → `scripts/run_daily.py` → `src/patterns/templates.py` → `src/screen/scorer.py` → `src/report/pipeline.py`。

---

## 5. 数据存哪（SQLite 7张表）

| 表 | 存什么 | 一句话 |
|---|---|---|
| `stock_meta` | 全A股票列表 | 代码/名称/板块/是否ST |
| `trade_cal` | 交易日历 | 判断今天是不是交易日 |
| `daily_bar` | 全部日K线（约1年） | 每只股票每天的开高低收+量，主原料 |
| `template` | ★赢家模板库 | 历史赢家"启动前25根"的形态（学习的成果） |
| `scan_result` | 每日选股结果 | 哪天、选了谁、分数、最像哪个模板 |
| `run_log` | 运行日志 | 每天跑得怎样 |
| `fetch_log` | 抓取日志 | 每只股票数据拉没拉到 |

---

## 6. 核心概念精讲（理解这套思路的关键）

### 6.1 "好股票"= 起步型赢家（锚点定义，templates.py）
不是涨幅最大，而是**未来才爆发、当天刚启动**的：
```
未来10日涨幅 ≥ 9%      （事后证明它会涨）
当天涨幅 ≥ 2%          （当天开始动）
前10日涨幅 < 3%        （之前没怎么涨 → 是初期不是中段）
前60日区间 -5%~25%     （排除一路狂奔/深跌反弹）
同一轮上涨只取第一个满足日
```

### 6.2 模板 = "启动前25根"的两种标准形态
- 价格形态：25根收盘价做 zscore 归一（消除价位差异，只看形状）；
- 量能形态：25根成交量/20日均量（量比节奏）；
权重 0.7/0.3，合成 0~100 的相似分。

### 6.3 选股打分
```
score = 形态相似分（当前25根 vs 模板库 Top-3 均值）
条件：score ≥ 60，取前 5；再叠加排雷过滤
```
> 曾有 pattern/signal/trend 三分量加权(0.40/0.35/0.25)，回测证明后两者是反指，已把权重改为 `{pattern_sim:1, signal:0, trend:0}`。

### 6.4 排雷（factors 的 filters，硬过滤不是打分）
避雷针（上影线≥振幅25%）、爆量(>150%)、暴热板块剔除、ST、涨停无买点。

---

## 7. 命令手册（什么时候敲什么）

```bash
# 第一次搭环境
pip install -r requirements.txt
python scripts/init_db.py
python scripts/backfill.py            # 全量回填(约1年)，可断点续跑

# 每天想手动跑完整流程（= 本地版"每日自动"）
python scripts/run_daily.py           # 更新数据→学形态→选股→出图文→推送

# 只想看某天选了什么（不联网、快）
python scripts/pick_top.py --date 2026-09-03

# 想知道信号灵不灵（★改参数后必跑）
python scripts/validate.py --days 20 --sweep   # 4种打分方式对比

# 看图
python scripts/visualize_templates.py --n 3    # 历史赢家的形态长啥样
python scripts/compare_pattern.py --rank 1     # 今天的Top1 和它最像的历史形态
python scripts/ascii_compare.py --rank 1       # 没图也能看的文字版

# 出报告/推送
python scripts/emit_report.py --no-push        # 只出图不推
python scripts/emit_report.py                   # 出图并推送

# 部署到 GitHub 自动跑
powershell -ExecutionPolicy Bypass -File deploy.ps1 -Repo "https://github.com/Roychaos/A-SHARE.git"
```

---

## 8. 怎么读懂验证结果（validate）

回放 = 假设回到过去每天做一次选股，再看"选中的股未来10日"到底涨没涨：
- **命中率**：选中后10日收益为正的比例；
- **随机基线**：闭眼随机选100只的命中率（衡量"行情本身好不好"）；
- **超额** = 选中组 − 随机基线（正数=你的信号真的有用）。

我们当前成绩：命中 ~69%，超额 +17~22pct（上涨/下跌市都为正）。

---

## 9. 调参总地图（都改 config.example.yaml）

| 想改什么 | 参数 | 例子 |
|---|---|---|
| "好股票"多严 | `learning.min_fwd_return` | 0.09 = 未来10日≥9% |
| 形态看几天 | `learning.window` | 25根 |
| 选几只 | `scoring.top_n` | 5 |
| 入选门槛 | `scoring.min_score` | 60 |
| 同行业上限 | `scoring.max_per_industry` | 2 |
| 排除哪些股 | `universe.*` | 剔ST/次新/低价 |
| 7因子/排雷 | `factors.*` | 阈值/开关 |
| 推送到哪 | `push.channels` | serverchan/wecom |
| 历史深度 | `fetch.years_back` | 1年 |
| 数据源 | `fetch.sources` | [eastmoney,sina] |

> 改完用记事本存成 UTF-8，或直接让我改；本地先 `validate --days 20 --sweep` 看效果再推 GitHub。

---

## 10. 推荐的学习路径（10步实操）

1. `pip install -r requirements.txt`（装好）
2. `python scripts/init_db.py` → 认识库
3. 看 `data/` 下生成了 `screener.db`
4. `python scripts/pick_top.py` → 第一次看到系统选股
5. `python scripts/ascii_compare.py --rank 1` → 看它"像谁"
6. 打开 `output/日期/` 里的卡片图 → 看图文长啥样
7. `python scripts/validate.py --days 10 --sweep` → 理解"命中率"概念
8. 打开 `config.example.yaml`，改一个参数（如 `top_n` 5→10），重跑第4步感受差异
9. 打开 `src/patterns/templates.py`，只读 `extract_templates` 函数，对照6.1的锚点定义
10. 打开 `src/screen/scorer.py` 的 `compute_and_select`，对照6.3的打分流程

> 仅供研究学习，不构成投资建议。
